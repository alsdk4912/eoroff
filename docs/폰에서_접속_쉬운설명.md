# 폰에서 쓰는 방법 (짧게)

## 같은 Wi-Fi (PC에서 개발 서버 켜 두고)

1. PC에서 `npm run dev:all`
2. 터미널에 나오는 **`Network`** 주소를 폰 브라우저에 입력  
   (v2는 보통 **5175** 포트, 예: `http://192.168.0.12:5175`)
3. **끝.** `.env` 없이 자동으로 같은 IP의 API(4015)에 연결됩니다.

---

## 밖에서 쓰기

→ **`RENDER.md`** (Render에 API + GitHub Secret)
