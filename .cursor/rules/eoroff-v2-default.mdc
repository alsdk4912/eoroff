---
description: EOR 휴가 앱은 기본적으로 버전2(eoroff)만 수정
alwaysApply: true
---

# 기본 대상: 버전2 (eoroff)

- **코드 위치**: `/Users/threedong/v2_project/surgical-leave-app-v2` (또는 GitHub 저장소 `eoroff`)
- **배포 URL**: `https://alsdk4912.github.io/eoroff/`
- 사용자가 **버전을 명시하지 않으면** `photo crolling/surgical-leave-app`(v1·`eor`)이 아니라 **항상 위 v2 프로젝트**를 수정한다.
- v1과 v2를 동시에 건드려야 할 때만 사용자에게 확인한다.

# 작업·배포 (에이전트가 할 수 있는 것은 에이전트가 처리)

- **git 커밋·푸시**(`main` 등), **로컬 빌드 검증**은 사용자에게 미루지 말고 **에이전트가 수행**한다.
- **사용자에게만 요청**: GitHub/Render **웹에서만 되는 설정**, **비밀번호·2FA**, **본인 PC 브라우저 새로고침·캐시** 등 도구로 불가한 것만 짧게 안내한다.
